import { APP_TAGLINE } from "../../constants/branding";

export const Footer = () => (
  <footer className="py-6 text-center">
    <p className="text-sm text-stone-500 tracking-wide">{APP_TAGLINE}</p>
  </footer>
);
